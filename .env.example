# H3C NetCtrl 环境变量配置
# 复制此文件为 .env 并填入实际值

# 日志级别: INFO / DEBUG
LOG_LEVEL=INFO

# Fernet 加密密钥 (python -c "from cryptography.fernet import Fernet; print(Fernet.generate_key().decode())")
ENCRYPTION_KEY=

# SQLite 数据库路径
DB_PATH=./data/dev.db

# 后端服务端口
BACKEND_PORT=8000
