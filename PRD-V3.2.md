# H3C NetCtrl V3.2 PRD：加固切换 + 大迁移

| 版本 | 日期 | 作者 | 说明 |
|---|---|---|---|
| V3.2 Draft | 2026-07-18 | 用户拍板 + Codex 共创 | V3.2 = 加固切换 + 大迁移：架构切 EVENG + 全 QA + VPC 全能力验证 |
| V3.2 Revise | 2026-07-19 | 用户拍板 + Codex 共创 | v3.2 拆分为 v3.2.1 新平台割接迁移、v3.2.2 新平台能力评级与回归验证；强调 OOB 管理地址迁移与旧/新平台配置适配 |

---

## 1. 背景与目标

### 1.1 背景

v3.0 骨架（sdn-vpc-netconf-schema-xml）已在真机 .5 / .26 验证**配置面** 100% 一致，但：
- **数据面**验证受 .26 设备限制无法做（用户原话："数据面在 26 上做不到的，就我们现在只管配置面就行"）
- **真机环境**模拟 L2/L3 不完整（underlay / OSPF / BGP 邻居由人工提前打通）
- **SDN 后端 QA** 体系不全（v2.4.2 / v2.5.0 的 QA 主要覆盖 ctrl + config + data，SDN 后端专项 QA 缺失）

### 1.2 目标

v3.2 = **新平台切换 + 大迁移 + 能力评级** 两个阶段：

1. **v3.2.1 新平台割接迁移**：用户通过 ZTP 将新平台设备逐台上线，提供“新 OOB 地址 ↔ 旧设备身份”映射；系统侧配合完成带外地址重编排、现网配置迁移适配与基础邻居/管理面恢复。
2. **v3.2.2 新平台能力评级与回归验证**：新平台作为后续主验证环境，系统性验证现有后端、前端、备份回滚、ZTP、SDN/VPC 下发与回收能力，形成能力评级和缺口清单。

### 1.3 用户原话

> "v3.2 我们会做一个是加固切换，3.2 叫加固切换，配合 ztp 的能力，做架构切换，切换到 eveng 平台。3.2 的时候呢，把所有的 qa 做了，就是包括 sdn 的后端现在已有能力，就是有一些没实现的能力先不管啊，已有能力先都做一遍。"

> "v3.2 要做一个什么点迁移完了之后我们去把现有的 3.0 实现的能力去... 看看能不能做到啊，vpc 全全能力好吧"

## 2. V3.2 范围与子版本

### 2.1 v3.2.1 新平台割接迁移

#### 2.1.1 目标

把新平台设备纳入 H3C NetCtrl 管理面，并把旧平台的关键业务配置迁移到新平台。该阶段本质是一次维护窗口内的割接动作，允许短时破坏业务，但要求过程可追踪、可回退、可人工确认。

#### 2.1.2 范围

- **新设备上线**：
  - 用户逐台触发 ZTP，上线新平台设备；
  - 平台侧只保证 ZTP 基础配置：OOB IP、OOB VRF、SSH、SCP、NETCONF、统一账号；
  - 用户在设备实际上线后提供映射关系，例如 `.103 -> 原 .2`、`.104 -> 原 .3`、`.105 -> 原 .100`。
- **OOB 地址重编排**：
  - 旧平台原 OOB 地址（如 `.2/.3/.100`）需要让位给新平台；
  - 旧平台设备的 OOB 地址整体迁到备份地址段，建议采用“原地址 +80”的可读规则，例如 `.2 -> .82`、`.3 -> .83`、`.100 -> .180`，最终以用户确认映射为准；
  - 该动作只影响带外管理面，不应改变旧平台业务侧 EVPN/VXLAN/OSPF/BGP 配置。
- **配置迁移适配**：
  - 不能简单把旧平台 `startup.cfg` 直接回滚到新平台，因为平台型号、接口名、OOB 口、物理连接可能不同；
  - 迁移策略为“对照旧配置与新平台配置，提取平台无关配置，适配平台相关配置”；
  - 可优先迁移相对平台无关的配置段，如 BGP、OSPF、EVPN、VPN instance、VSI、Vsi-interface、route policy 等；
  - 需谨慎处理平台相关配置段，如物理接口名、管理口、端口 service-instance、接口速率/模式、设备型号差异命令。
- **双平台隔离验证**：
  - 旧平台与新平台业务网络不互通时，可允许 OSPF/BGP/Loopback/业务地址在两边短期重复；
  - 新平台完成配置后，先独立验证 underlay/overlay 邻居、EVPN 路由、ARP/MAC、VPC 连通性；
  - 确认新平台可承载后，再逐步降低旧平台关注度。

#### 2.1.3 设计决策

- **映射来源**：设备物理位置与业务身份由用户人工确认，不由平台自动推断。
- **迁移粒度**：以设备为单位做配置迁移，以配置段为单位做适配，避免整份配置盲目回滚。
- **OOB 优先**：先处理带外地址可达性和资产匹配，再处理业务配置迁移。
- **业务地址可重复**：在旧/新平台隔离前提下，允许业务侧地址重复，降低迁移复杂度。
- **回退策略**：旧平台保留在备份 OOB 地址段，作为割接失败时的参考与回退入口。

#### 2.1.4 验收标准

- [ ] 新平台设备全部通过 ZTP 上线，SSH/SCP/NETCONF 可达
- [ ] 用户提供并确认“新 OOB 地址 ↔ 旧设备身份”映射表
- [ ] 旧平台 OOB 地址迁入备份地址段，且仍可登录
- [ ] 新平台设备使用原业务身份恢复关键配置
- [ ] 新平台 underlay/overlay 邻居与 EVPN/VXLAN 状态符合预期
- [ ] 新平台基础业务连通性通过人工/工具验证

### 2.2 v3.2.2 新平台能力评级与回归验证

#### 2.2.1 目标

在新平台上对 H3C NetCtrl 现有能力做一次系统性评级：哪些能力可直接生产使用，哪些需要适配，哪些暂不支持。该阶段不再重点关注旧平台，新平台成为后续 v3.x 的主验证环境。

#### 2.2.2 范围

- **管理面能力评级**：
  - 设备列表、设备详情、接口查看、VPN instance、命令执行；
  - 资产采集、CMDB 展示、Dashboard 统计；
  - 备份、回滚、SCP 推拉、重启等待。
- **ZTP 能力评级**：
  - 默认新设备上线；
  - recovery bypass；
  - 恢复默认序列；
  - ZTP 后自动纳管与资产可见（仅新设备主线）。
- **SDN/VPC 能力评级**：
  - 当前已有但未完整前端化的 SDN 后端接口；
  - VPC 创建、配置计划、下发、回收；
  - 端口绑定/解绑；
  - L3VNI / EVPN route / ARP / MAC 状态采集与校验；
  - 新平台下发后真实配置与期望配置对比。
- **前后端回归**：
  - 前端现有页面在新平台数据下是否可用；
  - 新平台设备加入后是否影响列表性能、资产采集、备份任务；
  - API 错误提示是否可读。
- **QA 补齐**：
  - 以能力评级发现的问题反向补单测/集成测试；
  - 不以固定“≥200 unit”为硬目标，优先保证关键链路覆盖和可复用 fixture。

#### 2.2.3 验收标准

- [ ] 形成新平台能力评级表：A 可用 / B 可用但有限制 / C 需适配 / D 暂不支持
- [ ] 管理面核心能力通过
- [ ] 备份/回滚链路在至少 1 台新平台设备通过
- [ ] ZTP 默认上线与 recovery bypass 均通过
- [ ] SDN/VPC create / bind / validate / cleanup 至少完成一轮闭环
- [ ] 所有 C/D 项有明确原因、证据和后续版本归属

### 2.3 VPC 全能力验证

#### 2.3.1 目标

v3.0 PRD 4 个子能力在 v3.2.2 集中验证：
- `sdn-vpc-prd-and-model`（数据模型）
- `sdn-vpc-foundation`（VPC CRUD）
- `sdn-port-binding`（端口随接随入）
- `sdn-l3vni-validation`（L3VNI 状态）

#### 2.3.2 范围

- **数据模型定稿**：
  - VPC / VSI / EVPN / L3VPN / Vsi-interface / service-instance 表结构
  - 关联关系（VPC ↔ 设备 / VPC ↔ 端口）
  - Alembic 010 迁移
- **VPC CRUD 后端**：
  - 租户 / VPC / 端口 / 路由 4 类资源 CRUD
  - 配置计划生成（dry-run / planned_config）
  - 业务下发（复用 v3.0 骨架）
- **端口随接随入**：
  - 端口绑定 service-instance 状态机
  - 自动检测端口 UP → 触发 VPC 接入
  - 前端展示端口归属
- **L3VNI 状态采集**：
  - RD/RT/EVPN route 采集
  - ARP/MAC 表采集
  - 校验闭环（VPC 配置 vs 实际状态）

#### 2.3.3 验收标准

- [ ] VPC / VSI / EVPN / L3VPN / Vsi-interface 数据模型定稿 + Alembic 010
- [ ] 4 类资源 CRUD API
- [ ] 配置计划 dry-run 准确率 100%
- [ ] 端口随接随入真机（.5 / .26）或 EVENG 验证
- [ ] L3VNI 状态采集 5min 内完成
- [ ] VPC 创建 → 端口绑定 → 状态采集 → 校验 闭环 100% 通过

## 3. 走法（建议）

| 阶段 | 主题 | 输出 | 依赖 |
|---|---|---|---|
| **阶段 1** | 新平台 ZTP 上线 | 新平台设备获得临时/静态 OOB，基础管理能力可达 | v3.1 |
| **阶段 2** | 映射表确认 | `.103 -> 原 .2` 等新旧设备身份映射 | 用户人工确认 |
| **阶段 3** | OOB 地址割接 | 旧平台地址迁入备份段，新平台接管原管理地址或目标地址 | 维护窗口 |
| **阶段 4** | 配置迁移适配 | 新平台恢复业务配置，平台相关配置完成适配 | 旧配置备份 |
| **阶段 5** | 新平台能力评级 | 管理面/ZTP/备份/SDN/VPC 全链路评级报告 | 阶段 4 |
| **阶段 6** | v3.2 发版 | RELEASE-NOTES + tag + push | 阶段 5 |

## 4. 不做（明确边界）

- ❌ **不做集中式网关降级产品化**（sdn-gateway-fallback 推 v3.3；v3.2 可作为排障动作临时使用）
- ❌ **不做前端大屏**（sdn-visual-overview 推 v3.4）
- ❌ **不做 etcd 协调**（sdn-etcd-coordination 推 v3.5 远期）
- ❌ **不做商用 R6607+ 设备 ZTP**（v3.1.1 不在 v3.2 范围）
- ❌ **不做全自动物理身份识别**（设备物理位置与旧设备身份由用户人工映射）
- ❌ **不做整份配置盲回滚**（新旧平台存在接口和平台差异，必须适配）

## 5. 依赖关系

- v3.2.1 依赖 v3.1 ZTP 基础配置和 recovery bypass。
- v3.2.1 依赖用户提供新旧设备映射表。
- v3.2.2 依赖 v3.2.1 完成新平台管理面与关键业务配置迁移。
- v3.2.2 依赖 v3.0 SDN/VPC 骨架和 v3.1 ZTP 能力。

## 6. 风险

- **风险 1：新旧设备物理身份映射错误**：可能把旧设备配置迁移到错误新设备。
  - 缓解：映射表必须由用户人工确认，迁移前二次核对设备型号、MAC、序列号、当前 OOB。
- **风险 2：整份配置回滚不兼容**：接口名、OOB 口、平台命令差异导致配置失败或半状态。
  - 缓解：按配置段迁移，先平台无关段，再平台相关段；每段保留 before/after 证据。
- **风险 3：OOB 地址冲突**：新旧平台管理地址冲突导致设备不可达。
  - 缓解：旧平台先迁入备份地址段，新平台再接管目标地址；每步验证 ping/SSH/NETCONF。
- **风险 4：SDN/VPC 骨架在新平台暴露问题**：v3.0 骨架可能只在配置面验证过。
  - 缓解：v3.2.2 通过能力评级将问题归类，不强行一次性修完所有缺口。
