# 前端环境变量模板（v24-feat-async-backup-status）
# 复制为 .env 并按需修改，.env 已被 .gitignore 忽略

# 异步备份/回滚（v24-feat-async-backup-status）
# true  = 启用异步模式：提交后立即返回，后台执行，右下角面板显示进度
# false = 同步模式（默认，v2.3 行为）：前端阻塞等待完成
VITE_ASYNC_BACKUP=true

# 后端运行模式：split（ctrl/config/data）或 core（单体 backend）
VITE_API_MODE=split
