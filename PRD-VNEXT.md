# H3C NetCtrl NEXT：下一代网络工作台产品蓝图

> 产品定位：以业务目标组织操作，以设备事实解释结果，以持续验证逐步走向意图驱动。
> 本文是跨阶段产品设计，不是一次发版的交付清单，也不是数据库或 API 定稿。

| 项目 | 约定 |
|---|---|
| 文档状态 | Draft v0.1，2026-09-06，待用户评审 |
| 产品负责人 | 用户：决定业务目标、优先级、范围与阶段验收 |
| 总体设计负责人 | Codex：产品架构、网络配置联动、前端设计与主实现、后端拆分方案审核 |
| 实现协作 | 后端及基础能力可由其他开发者 / AI 分块实现，按验收结果交接 |
| 版本关系 | NEXT 是规划代号，不代表 v4.0，不替代 v3.4；具体版本归属另行确认 |
| 文档管理 | A 类产品蓝图；文档 change 为 `next-product-blueprint`，不授权执行设备变更 |

## 阅读指引

- 了解长期方向：读 §1、§4、§5、§8。
- 理解分工与技术边界：读 §2、§6、§7、§9。
- 准备一期开发：读文末 §11，先核实基线，再提出小范围实施方案。
- 未来能力不因写入 PRD 而成为一期前置条件；后续阶段启动时重新评估。

## 1. 产品愿景

### 1.1 我们要解决什么

现有平台正在完善设备管理、配置下发、VPC 接入、备份和 ZTP 等基础能力。下一步的价值不是把这些功能全部搬进更复杂的页面，而是让用户连续回答三个问题：

1. 我希望这个业务网络是什么样？
2. 它现在实际是什么样？
3. 两者哪里不同，我为什么可以相信这个判断，接下来可以做什么？

用户不应先学习底层配置顺序才能完成接入；网络工程师也不应因为界面“简化”而失去深入分析的入口。

### 1.2 目标体验

从 VPC 进入，看到其覆盖位置、接入终端和状态。选择一个对象，即可沿关联深入到逻辑网络、网关、设备和端口；选择一次变更，即可查看影响范围、执行过程与验证证据。切换观察方式后，对象、范围和时间上下文保持一致。

这里的“下一代”指更强的理解、决策与追溯能力，不等同于三维画面、聊天入口或更多动画。

### 1.3 IBN 的位置

IBN（Intent-Based Networking，意图驱动网络）强调描述目标而非逐条指定实现步骤，并将目标实现与后续保障联系起来。它不要求用户通过自然语言输入，也不以大模型为必要前提。[IRTF RFC 9315 §3、§5](https://www.rfc-editor.org/rfc/rfc9315.html#section-3.1) 是概念研究文档，不是本项目已通过的标准认证。

本项目借鉴这条路径：业务目标 -> 范围和约束 -> 执行方案 -> 设备落地 -> 证据校验 -> 偏差处理。

一期只建设可理解、可验证的操作闭环。有限范围的长期意图保障在后续阶段建设，不把服务表单或一键下发直接宣传为完整 IBN。

## 2. 项目基线与边界

### 2.1 可以复用的基础

以下是本次轻量核对得到的设计基线，不替代后续实施前的接口与真机验收：

| 基础 | NEXT 的使用方式 | 当前证据入口 |
|---|---|---|
| ctrl / config / data 拆分 | 复用身份、执行、采集与存储职责 | [README 当前架构](README.md) |
| 租户、VPC、端口绑定 | 业务视图的对象基础，不另造一份孤立资产库 | [资源模型](backend/app/models.py) |
| VPC 下发 / 撤回、端口绑定 / 解绑、网关局部操作 | 作为经过准入和影响确认的操作能力 | [SDN 主规格](openspec/specs/sdn/spec.md) |
| 部署记录、父子 unit、校验快照 | 事件关联与证据展示的起点，不视为完整事件历史 | [资源模型](backend/app/models.py) |
| display 状态采集、网关源地址 ping 验证 | 支撑限定范围的观察和可达性结论 | [采集服务](backend/app/services/sdn_validation_collector.py)、[README](README.md) |
| NETCONF/XML + SSH 混合通道 | 沿用设备适配能力，不把通道统一化作为 NEXT 前置条件 | [总体蓝图](PRD-V3.0.md) |
| v3.4 SDN/VPC 工作台 | 作为基础产品能力来源，启动一期时核实最终交付契约 | [工作台 PRD](PRD-V3.4.md)、[路线图](VERSION-ROADMAP.md) |

本次编写期间，v3.4 开发与收尾文档也在并行更新。本文不覆盖其成果，不单独认证其完成状态。启动一期须记录采用的提交 / 版本和已确认契约；历史 PRD、路线图旧段落与代码状态不一致时，先记录核对结果，不自行挑选最有利的口径。

### 2.2 不应误认的能力

- 默认 600 秒缓存 / 最小采集间隔不等于已有每 10 分钟自动运行的调度器。
- deployment 的父子关系、当前状态和更新时间不等于每个中间事件都已持久化。
- ARP、MAC 和路由表可以提供接入与转发线索，不等于真实线缆拓扑或逐包路径。
- 设备返回配置成功、主机地址已学习、本地网关探测通过、跨 Leaf 互通，是不同层次的结论。
- 现有实验记录包含本地下联验证；不能把尚未验证的远端同 VNI 互通宣称为已证明。
- 设备命名、型号、LSTN/RSTN 平台类型不能代替 EVPN Fabric 成员资格。

### 2.3 保持的产品边界

underlay、OSPF 和 BGP 邻居仍由人工准备；NEXT 初期不接管这些配置。ACL、多厂商全面适配、平台迁移、全网实时流量分析、自动恢复整机配置均非一期内容。

ZTP、备份回滚、CMDB 保留现有独立职能。工作台可以带上下文跳转，但不重复实现或自动串联这些高影响动作。管理 / OOB 接口不进入业务接入与撤回操作范围。

## 3. 用户与核心场景

| 用户视角 | 主要问题 | 产品响应 |
|---|---|---|
| 网络使用者 | 这个网络覆盖哪里，我怎么接入终端？ | 以 VPC、位置、地址和结果为主，不暴露默认配置顺序 |
| 网络维护者 | 哪个对象异常，配置和状态是否一致？ | 按关联下钻，展示设备事实、技术字段和原始证据 |
| 变更执行者 | 会影响谁，哪里成功了，失败后怎么继续？ | 影响预览、分步结果、受控重试与明确撤回范围 |
| 项目维护者 | 能力来自哪个模块，能否交给别人实现？ | 稳定产品语义、边界与验收场景，避免依赖对话记忆 |

长期覆盖四类旅程：创建业务网络；接入和扩容；检查与解释异常；按范围撤回、补回或维护。每个旅程独立形成可用结果，不要求四类同时完成。

## 4. 产品原则

1. **业务先行，技术可达**：默认用业务语言；展开后可以看到 VRF、VSI、VNI、RD/RT、AC 和采集输出。原 PRD 的“隐藏专业术语”在 NEXT 中解释为默认隐藏，而非禁止深入。
2. **期望、事实、推断分开**：每个重要状态标明依据、时间和验证范围。未知不能显示成正常，过期不能显示成实时。
3. **同一对象贯穿视角**：地图、列表、事件链、分层视图共享对象身份，不以名字或画布坐标关联业务。
4. **动作有范围，有相反动作**：设计创建、绑定、撤回时同时考虑补回、解绑、引用关系和中间失败；不保证所有逆向操作都是自动回滚。
5. **用户可以改变目标**：主动撤回与维护操作不能被自动当作故障修复。共享网关或租户对象不能随单个端口删除。
6. **先进体验建立在证据上**：动画表达关系或记录中的事件，不伪装成实时包流、精确故障位置或真实时间旅行。
7. **渐进增强**：前端体验可先提升，后端按所需语义增量补齐；不为视觉效果先更换数据库、框架或拆新容器。
8. **阶段独立可用**：一期没有完成远期愿景不是失败；一期缺失自身验收条件不能用“后面再做”掩盖。

## 5. 前端产品设计

### 5.1 一个工作台，多种观察方式

ATLAS、PULSE、STRATA 是本轮概念样机的三个观察视角，不是三套最终产品或三份后台数据。名称、配色和布局仍可迭代，保留以下体验责任：

| 视角 | 负责的问题 | 演进方向 |
|---|---|---|
| 业务空间 / ATLAS | 网络里有什么，在哪里，能做什么？ | 日常操作主入口；VPC、覆盖位置、终端、状态与接入动作 |
| 事件过程 / PULSE | 一次动作经过什么，为什么停在这里？ | 围绕真实变更组织事件、结果、证据和后续动作 |
| 分层透视 / STRATA | 这个业务对象依赖下面哪些东西？ | 业务、逻辑网络和设备承载之间的关联解释；按需进入三维 |

样机源文件目前在开发机 `/root/workpace/netctrl-concepts/`；它们是离线演示数据，不是运行系统或交付证据。此目录不随仓库分发，缺少样机时仍应能独立阅读本文；正式前端实施 change 必须补充可复用的交互图与截图。

### 5.2 信息位置

| 区域 | 稳定职责 | 避免的问题 |
|---|---|---|
| 平台导航 | 业务工作台、资产、运营工具等入口 | 不把下一代工作台做成脱离原平台的第二套门户 |
| 左侧业务范围 | 租户 / VPC 选择、搜索、创建入口 | 不把所有 Leaf 列表当作首页主结构 |
| 顶部上下文 | 当前 VPC、网段、覆盖摘要、主要动作 | 创建 VPC 与接入终端位置明确，不藏在下方配置面板 |
| 中央工作区 | 业务关系、对象聚焦、范围或状态对比 | 信息随观察目的变化，不堆叠所有技术标签 |
| 右侧详情 | 当前对象或当前步骤的解释、依赖、证据和操作 | 不让用户为确认一个状态来回跳多个页面 |
| 下方记录区 | 终端列表、变更记录、校验结果 | 数据量增长时可筛选、展开，不挤压主要操作区域 |

详细分析允许中央区域展开为事件或分层视图，退出时返回原 VPC、选中对象和筛选条件。跨对象 / 跨 VPC 跳转须显示范围变化，不能沿用旧对象的证据。

### 5.3 当前态、目标态、差异

长期支持三种语义视图：

- 当前态：最近观察到的状态，明确采集时间、失败范围和缺失信息。
- 目标态：用户确认并保存的业务要求，不把前端临时表单当作已生效目标。
- 差异：有依据的缺失、多余、不一致或未验证项；允许深入到对应设备对象。

一期只展示本次操作的预期变化与结果，不建设全网长期目标版本库。只有后续具备可靠目标记录时，才开放持久化目标态与漂移对比。

### 5.4 专业分析仍然保持体验完整

技术字段采用对象化呈现：网关和 L3VNI 的依赖、端口和 AC 的归属、主机的学习位置，而不是默认展开一屏终端输出。原始输出作为可展开证据保留，不用漂亮图形替代核验。

三维仅在有助于解释跨层关联时使用，保持二维 / 列表替代路径。一期不以正式三维引擎作为交付门槛；后续引入时，由 Codex 负责构图、选择、聚焦、布局稳定性、性能与无障碍替代体验。

颜色同时配合图标与文字表达状态；设备 IP、网段、端口名必须可读，不依赖缩小浏览器缩放比例。延迟、空数据、部分失败、维护、只读对象均为正式设计状态。

## 6. 业务语义与控制边界

### 6.1 核心语义

以下是产品含义，不是新增表、接口或枚举的定稿：

| 概念 | 必须表达的含义 |
|---|---|
| 业务对象 | 租户、VPC、接入位置、终端及稳定身份；设备侧资源继续由现有规则映射 |
| 覆盖范围 | 希望部署、已执行部署、已观察确认是三个不同集合 |
| 关联关系 | 业务归属、逻辑依赖、接入位置或物理邻接；说明来自计划、配置、采集还是推断 |
| 变更 | 用户请求、影响边界、执行尝试、已完成部分和验证结果的统一关联 |
| 证据 | 来源、设备 / 对象范围、采集时刻、结果、支持哪些结论及限制 |
| 目标 / 例外 | 用户希望保持的状态，以及主动排除、维护或暂不管理的范围 |

沿用本项目的资源粒度：一个 VPC 对应一个业务子网，不引入云平台式的多层嵌套子网结构。既有编号分配、部署覆盖和资源复用规则不因展示方式变化而被重定义。

不使用 RD 作为数据库租户身份，也不把 RD 不同等同于隔离已经成立；路由导入 / 导出、VRF 和 VNI 映射继续受既有规格与设备验证约束。

### 6.2 状态不能压成一个绿灯

展示至少区分：操作是否执行成功；配置是否被回读确认；业务验证是否通过；证据是否新鲜且完整。它们可以聚合成简要状态，但详情必须保留各维度。

待接线、待验证、采集失败、证据过期、不支持检测均不是“业务已失败”的同义词。反之，采集正常也不等于业务正常。

### 6.3 成员资格与变更所有权

- 业务写操作只面向经后端确认的 EVPN Leaf 成员；前端筛选不能替代后端准入校验。v3.4 的 `sdn_role=evpn_leaf` 在一期基线核实时确认实际契约。
- 对成员做出的资格认定不等于设备当前配置 / 邻居已健康，仍须预检。
- 新增成员不因出现在资产列表就自动触发全网配置下发；覆盖范围变化要有可确认的变更。
- 已有非平台管理的配置可作为观察对象；纳入管理需要明确映射和所有权，不从设备数据自动补建租户 / VPC 并立即接管。
- 撤回只处理本次或明确选定范围内的平台管理对象；遇到已有或共享资源，不推定可以删除。

### 6.4 维护、例外与后续自治

一期不自动修复漂移。后续建设目标保障时，人工撤回、网关停用、维护暂停须影响目标判定，记录作用域、操作者和恢复策略。

例外结束应先重新观察并形成差异，不默认立即回填配置。目标变化或其他操作已改变对象时，旧的修复计划必须重新确认。

## 7. 后端演进方向

后端各模块的选择权留给实施者，产品要求围绕四类能力提出：

| 能力 | 产品需要 | 约束 |
|---|---|---|
| 对象关联与读取 | 同一对象可在业务、技术、事件视角中识别和追溯 | 优先聚合已有身份与资源数据；不要求图数据库 |
| 变化与执行 | 表达计划范围、依赖、执行结果、重试和撤回边界 | 复用现有下发服务；不另造平行 CLI 执行器 |
| 观察与解释 | 将快照转成有来源的结论与检查建议 | 保留原始证据引用；推断不是事实，相关性不是根因 |
| 目标保障 | 长期对比目标与实际状态，识别偏差并受控处理 | 后续按场景逐步建设，不要求一期通用自治 |

原则上沿用 ctrl / config / data 的职责与既有服务交互。是否新增模块、表、异步队列、调度器或容器，由对应阶段的实施设计说明依据，经过审核后决定；本蓝图不预先规定。

Agent 将来可作为表达目标、解释证据和生成候选方案的辅助入口。它不能绕过目标校验、成员准入、操作确认、管理接口保护或执行审计。没有 Agent 时核心工作流仍须完整可用。

## 8. 分阶段演进

阶段按能力成熟度划分，不是固定工期、固定版本号或固定数量的 OpenSpec change。

| 阶段 | 用户获得的独立价值 | 主要建设 | 本阶段不承诺 | 下一阶段的启动条件 |
|---|---|---|---|---|
| S1 可信业务工作台 | 看清一个 VPC，完成一次终端接入并解释结果 | 业务关系、有限技术下钻、单次操作预览 / 记录 / 证据，详见 §11 | 全网目标库、持续自治、正式三维全景 | 用户认可工作流和视觉方向；真实动作闭环通过验收 |
| S2 多视角与差异 | 从业务深入底层，比较目标和观察到的差异 | 目标范围与例外、双态对比、跨层剖面、多对象变更解释 | 任意问题的自动根因分析或自动修复 | 关系与差异可信；需要保障的有限目标明确且能被验证 |
| S3 受限意图保障 | 目标在后续检查中持续被核验，偏差有处理路径 | 有界周期 / 事件检查、漂移识别、建议方案、人工确认修复 | 开放式自然语言自治、无条件自愈、不可观测的 SLA | 对目标冲突、维护状态和修复风险已有可验证的边界 |
| S4 可选智能协作 | 更方便表达目标、解释复杂事件或评估候选方案 | 可插拔 Agent、扩展检查能力、经单独授权的有限自动处理 | 全场景无人运维 | 独立收益与成本评估；不作为 S1-S3 的前置依赖 |

每阶段结束可以稳定停留，不因为后续阶段存在就强行推进。S2 及之后的范围在启动前重新写阶段实施说明，不提前冻结所有库表、API 与任务拆分。

## 9. 团队协作与设计审核

### 9.1 责任分工

| 责任 | 默认负责人 | 交接要求 |
|---|---|---|
| 产品目标、优先级、真实场景与窗口 | 用户 | 决定阶段是否进入实现、是否可以开始收尾 |
| 总体产品设计、范围与关键网络语义 | Codex | 审核跨模块联动、可观测性、影响范围和验收逻辑 |
| 前端交互、视觉与主实现 | Codex | 持续主导审美与交互落地；委托局部工作后仍负责一致性验收 |
| 后端方案与实现拆分 | 后端开发者 / AI 提出，Codex 审核 | 不限制具体任务数量，但每块须对齐业务结果、依赖和可验证边界 |
| 测试与事实核验 | 实施者负责，Codex 汇总审核，用户参与实景验收 | 自动化、真机与视觉证据分开，不以其中一类替代全部 |

角色以职责延续，不依赖特定会话永久持有上下文；交接材料必须让未参与讨论的开发者独立理解。

### 9.2 两次必要审核

实施前：确认该块解决哪个用户场景、复用什么能力、改变哪些业务语义、缺少什么验证条件。后端拆分尤其要交代成员准入、资源复用、范围、幂等、并发、部分失败和证据关联。

联调后：前端是否正确表达后端事实，后端是否真正做到画面承诺的效果；同时核对未完成范围和对既有能力的影响。

各块按项目 OpenSpec 流程推进，不为本蓝图另建平行流程。产品草案、实施规格、实际测试结果分别记录；实施验证推翻蓝图假设时，先更新决策，再调整计划。

### 9.3 收尾与确认

**开始写阶段收尾结论、归档或准备发版前，先向用户确认是否可以收尾，并收集额外需求。** 提交、推送和 tag 继续按既有确认要求执行，不把“文档写完”当作“需求已获确认”。

本轮只形成 Draft 文档。后续可以分块提交已确认成果，不要求等整个 NEXT 蓝图全部实现后才提交；也不以单块提交代表阶段完成。

### 9.4 一期双向交接入口

当前入口：[一期协作交接](openspec/changes/next-product-blueprint/collaboration.md)。后端开发者从该文档接手，引用本 PRD §11 提出复用 / 缺口和实施方案；Codex 在同一记录中回复审核结果。用户不负责转述技术方案，只在需要取舍和验收时决定。

本 PRD 负责产品边界，协作交接记录待审事项与下一步负责人，各实施 change 负责具体方案、任务进度和验证证据，不维护重复的 PRD 或任务表。Codex 每次继续或收到审核请求时先读取交接记录；它不是自动监听机制，更新后仍需有消息或下一次任务触发读取。

实施 change 确定或蓝图文档归档时，维护者更新此处到唯一的当前协作入口，保留历史来源，不让后续开发依赖旧会话记忆。

## 10. 风险、待决策与依据

### 10.1 风险处理

| 风险 | 处理原则 |
|---|---|
| UI 的确定性超过采集能力 | 每个状态必须有范围和依据；无法观察的部分明确未知 |
| 为视觉增加高频 SSH | 区分界面读取、任务状态更新与设备采集；后者必须受控 |
| 多人开发导致重复模型或两套执行链 | 先审核契约与复用边界，统一对象身份与执行归属 |
| 长期蓝图被当成一期工单 | 一期以 §11 为边界；扩围须重新确认，不静默追加 |
| 新旧工作台同时写入 | 使用同一后端业务服务，处理重复提交、过期计划和并发变化 |
| 模拟器限制妨碍验证 | 分开记录已证实、受限、未验证；不伪造结果，也不凭本地成功外推全网 |

### 10.2 待用户 / 实施评审确认

- 一期切入：建议采用 §11 的“已有 VPC 接入与验证”，本次草案尚未视为用户定稿。
- 版本归属：NEXT 各阶段与当前 v3.4 后续版本的关系，不在本文擅自编号。
- 真机环境：一期启动时确认成员、端口、终端、管理保护范围及维护窗口；历史测试地址不是新的长期写入授权。
- 技术选择：关系读取契约、操作记录方式、迁移与前端展示库由实施设计决定，不在 PRD 提前固化。

### 10.3 依据与阅读入口

- [项目说明](README.md)、[版本路线图](VERSION-ROADMAP.md)、[VPC 总体蓝图](PRD-V3.0.md)：复用现有业务边界和能力，不抹去历史。
- [项目规则](.trae/rules/project-convention.md)、[QA 规则](.trae/rules/qa规范.md)：文档、工具、测试与收尾规则继续适用。
- [IRTF RFC 9315](https://www.rfc-editor.org/rfc/rfc9315.html)：意图、实现、保障及生命周期的概念参考。
- [IRTF RFC 9316](https://www.rfc-editor.org/rfc/rfc9316.html)：意图分类参考；不据此宣称产品具有通用意图理解能力。

---

## 11. 一期：可信业务空间与终端接入闭环

> S1 是建议的一期范围，待用户确认后由实施者拆分 OpenSpec change。它不是对全蓝图的压缩实现。

### 11.1 一期目标与演示故事

**用户在一个工作台内，理解一个已纳管 VPC 的接入情况，完成一次终端扩容，并能判断操作和业务验证分别是否成功。**

参考故事：选择研发网络，选择一个有资格且具备前置条件的 EVPN Leaf 和空闲业务端口，输入用于验证的终端地址，预览本次范围后开始接入。平台完成受控端口配置，用户完成接线和终端 IP / 网关配置，再点击完成验证。无论成功或失败，用户都能查看对应证据并继续处理。

终端没有 SSH 不阻塞这一有限故事；可通过已有网关源地址探测与设备学习信息验证可观察的部分。本地网关到终端的成功不代表任意跨 Leaf 或应用层通信成功。

### 11.2 启动前的基线核对

一期实施方案须先确认：

- v3.4 已确认 / 合并的接口、成员角色与前端入口；没有合并的能力明确作为依赖，不照抄并行工作副本。
- 现有 VPC、端口、扩容、下发与撤回接口的实际语义；通过最小契约测试核对是否需要补齐。
- 至少一个平台管理的实验 VPC、一台合格 EVPN Leaf、一台可接入终端及获准使用的业务端口。
- 对照原工作台记录一次接入的步骤、需手动查命令的次数和主要困惑，作为一期体验比较基线，不虚构提升百分比。

上述条件不齐时，可以先完成契约和交互设计，但不能宣称真实一期闭环已经验收。

### 11.3 一期必须交付

| 编号 | 用户得到的能力 | 前端要求 | 后端复用或增量要求 |
|---|---|---|---|
| N1-01 | 找到 VPC 及其接入情况 | VPC 选择、业务关系画布、终端列表、对象聚焦；IP / 端口完整可读 | 聚合现有身份、VPC、绑定、部署与快照；关联来源明确 |
| N1-02 | 看清“知道什么、不知道什么” | 区分配置结果、业务验证、缓存 / 过期 / 未知；显示时间和作用范围 | 提供结论与证据关联、采集时间、缺失和失败范围；不让前端自行猜测状态 |
| N1-03 | 预览一次新增接入 | 选择合法成员与业务端口，填写终端地址，显示修改项和保留项 | 成员与端口准入、地址与绑定检查、目标 VPC 前置检查；不足时阻止执行并说明 |
| N1-04 | 执行接入并保留进度 | 操作可追踪；配置完成后进入待接线 / 待验证，不直接显示业务成功 | 复用扩容 / 绑定执行链，关联请求、尝试、设备、结果；重复提交不重复执行 |
| N1-05 | 接线后验证结果 | 完成验证后显示本次主机学习与网关探测结果，失败可定位到未通过项 | 调用现有适用检查，关联本次操作后产生的证据；不能用变更前缓存证明成功 |
| N1-06 | 深入分析当前对象 | 业务 -> 逻辑依赖 -> 设备 / 端口详情联动，可展开技术字段与脱敏原始证据 | 返回本次支持的关联和证据；尚未发现的物理邻接不生成虚假链路 |
| N1-07 | 理解过程与失败后续 | 一条真实操作记录的步骤、结果、重试 / 重新验证入口 | 持久保存足以恢复进度的记录；明确已完成部分；不从一个最终状态伪造中间事件 |
| N1-08 | 撤回本次新增接入 | 再次确认范围，区分停止后续操作与撤回已落地绑定 | 复用受控解绑；仅处理本次新建且无新引用的绑定；保留 VPC、网关、租户和其他端口 |
| N1-09 | 新旧入口可共存 | 集成现有平台导航、语言与交互基础；带上下文返回原功能 | 共用业务规则与身份，处理旧入口造成的状态变化；不建立第二套写接口逻辑 |
| N1-10 | 交互可用而非仅效果图 | 完整加载、空态、错误态、待处理态、键盘焦点与窄屏布局 | 支持稳定返回缺失 / 不支持信息，不把错误隐藏为成功空列表 |

N1-03 的终端 IP 用于本次验证，不代表平台向终端配置 IP 或提供 DHCP。该一期故事要求输入有效主机地址；仅浏览已有绑定不强制补填历史终端地址。

目标 Leaf 尚未具备该 VPC 时，一期说明缺少前置部署，并带上下文进入既有部署流程；不能把一个端口接入确认静默扩大成全设备 VPC 部署。

### 11.4 一期操作流程

| 步骤 | 可见结果 | 关键边界 |
|---|---|---|
| 选择网络与对象 | 当前已知覆盖、接入位置、终端及证据时间 | 未管理对象只观察，未知不变成正常 |
| 填写接入位置与地址 | 合法设备、端口、VPC 归属与可验证地址 | 不默认选择全部设备；不提供 OOB 业务改配 |
| 预览并确认 | 将新增什么，保持什么，存在什么阻塞 | 预览本身不下发；范围或前置状态改变后须重新确认 |
| 执行配置 | 本次执行步骤与设备结果 | 已完成、未完成和失败范围分开 |
| 等待用户接线 | 待接线 / 待验证状态 | 不因等待而制造成功或在后台自动改终端 |
| 发起验证 | 检查结果、证据与作用范围 | 用户操作后采集；采集失败不自动等同于终端故障 |
| 结束或继续处理 | 验证通过、未通过或证据不足；可重新检查 | 不自动重放已经成功的配置步骤 |
| 必要时撤回 | 本次新增绑定移除及回读结果 | 共享资源或对象已变化时阻止不安全撤回并解释 |

### 11.5 一期后端需要补齐的最小语义

这不是接口路径或库表设计。实施者可以复用、扩展或聚合现有能力，但须覆盖：

1. 对象与关系的稳定身份、业务范围、来源；展示身份与真实设备身份可追溯。
2. 操作请求与执行尝试的关联、关键阶段结果、更新时间，支持刷新页面后继续查看同一操作。
3. 校验结果与对应快照 / 探测的关联，区分新采集与缓存命中，暴露证据范围和缺失项。
4. 预览到执行之间的有效性检查，以及重复点击、其他入口操作或资源变化时的安全反馈。
5. 本次新增资源与既有资源的界限，确保撤回可审计且不越界。

无法可靠关联的历史记录按“历史信息不完整”展示，不补造事件、时间或目标。新增记录的保留方式须在实施规格说明，不把父对象删除后的历史可追溯性留给前端猜测。

一期采用手动同步与动作触发验证，保留受控缓存策略。页面刷新、视图切换与查询任务状态不得隐式触发高频设备采集；操作后验证须获得足够新的证据。具体失效 / 强制采集策略由后端设计提出并审核。

### 11.6 一期明确不做

- 新的一套 VPC / 租户创建引擎、编号分配规则或全网部署引擎；已有能力从既有入口使用。
- 新增网关局部改配、全 VPC 批量撤回、任意范围的图形编排；现有功能不被删除，但不是一期新交互的必交付。
- 全网长期目标版本库、通用差异引擎、周期自治保障与自动修复。
- 正式三维全景、物理布线自动发现、逐包动画、任意历史时刻重建。
- LLM / Agent 接入、自然语言配置下发、框架整体迁移、强制新容器或数据库升级。

因此，一期不是把三款样机全部产品化：先兑现业务空间、单次事件与有限下钻；STRATA 完整剖面留在 S2 细化。

### 11.7 可委托的工作块

以下是能力边界建议，不固定 change 数量、接口数量、工时或开发者内部做法：

| 工作块 | 建议负责人 | 可交接成果 | 依赖 |
|---|---|---|---|
| 体验与契约对齐 | Codex 主导，后端参与 | 关键页面状态、一期故事、对象与结果语义、可点击交互稿 | 基线核对 |
| 关系与证据读取 | 后端实现，Codex 审核 | 可测试的数据契约，正常 / 未知 / 过期 / 部分失败样本 | 体验契约 |
| 操作过程与安全边界 | 后端实现，Codex 审核 | 复用执行链的过程记录、验证关联、重复 / 并发 / 撤回处理 | 同一对象与操作语义 |
| 下一代工作台主实现 | Codex | 真实接口联动、业务画布、上下文详情、操作记录与完整状态 | 契约可先用 fixture，验收必须接真实服务 |
| 集成与实景验收 | 实施者配合，Codex 主导核对，用户验收 | QA、真机、视觉结果和未验证范围 | 前后端闭环可运行 |

每份小规格至少引用一个 N1 编号，说明输入 / 输出语义、复用边界、失败处理和测试证据。各块可合理并行，但不能分别发明自己的对象身份、状态含义或执行链。

### 11.8 验收场景

| 场景 | 必须看到的结果 | 对应要求 |
|---|---|---|
| 正常接入 | 从选择已有 VPC 到端口执行、待接线、验证通过，可关联本次记录与证据 | N1-01、N1-03、N1-04、N1-05、N1-07 |
| 地址学到但 ping 未通过 | 显示学习成功与连通失败两个结果，不将 VPC 全部染绿，也不自动断言网关根因 | N1-02、N1-05、N1-06 |
| 配置或采集部分失败 | 清楚保留成功部分，说明未知 / 未执行部分，重试不重复应用已完成动作 | N1-02、N1-04、N1-07 |
| 验证使用旧快照 | 可识别旧证据并发起合适的新检查，旧缓存不能证明新操作已成功 | N1-02、N1-05 |
| 重复提交或中途刷新 | 同一操作可以继续追踪，不新增重复绑定或丢失当前进度 | N1-04、N1-07 |
| 非 EVPN 成员、OOB 或不合法业务端口 | 前端明确提示且后端拒绝执行；不依赖设备名称判断 | N1-03、N1-09 |
| 预览后对象被其他入口改变 | 执行前重新校验，失效计划不能照常执行 | N1-03、N1-09 |
| 主动撤回本次接入 | 只撤回获准的新绑定；不删除共享 VPC / 网关，不自动再次补回 | N1-08 |
| 缺少物理邻接或终端学习信息 | 明确未知，没有凭空补出的设备连线或终端位置 | N1-01、N1-02、N1-06 |
| 中英文与桌面 / 窄屏 | IP、端口、按钮文字不被遮挡；切换视角不丢对象上下文 | N1-09、N1-10 |

### 11.9 验证与阶段出口

- 自动化：沿用 QA 容器完成契约、解析、状态、重复执行、异常和前端回归；真实 display 输出准备 fixture，不让常规测试依赖在线设备。
- 视觉：由 Codex 实际点测并保留截图；至少核对 1280x800、1440x900 桌面和 390px 窄屏。窄屏可改为堆叠布局和局部画布滚动，不以整体页面溢出或缩小浏览器作为解决办法。
- 真机：经本轮授权，在隔离实验资源完成至少一条接入、验证、受控撤回链路；只通过项目工具与业务 API 操作。记录设备、端口、时间、证据、清理结果和既有对象未被改动的核验。
- 验证强度：fixture / Mock 证明实现行为，真机证明实际能力，两者不能互相替代。环境不足时可以验收局部工作块，一期真实闭环仍标记待验证。
- 覆盖范围：没有跨 Leaf 终端验证条件时不宣称跨 Leaf 端到端成功；一期允许只证明本地接入故事，但必须在验收中明确范围。
- 用户体验：用户能从业务入口完成上述故事，并在异常后找到依据和下一步，不需要先翻原始命令才能理解平台是否完成了动作。
- 收尾：先展示实际结果，询问用户是否可以开始收尾及是否有追加需求，再处理阶段结论、归档、发布和后续阶段安排。

一期完成的含义：一个边界清晰、能在真实设备上解释结果的新工作台已经可用。它不表示所有 VPC 能力、完整三维剖面或 IBN 愿景已经实现。
