# PRD\-基于NETCONF的H3C网络控制平台（个人自研项目）V1\.0

## 版本记录

|版本|日期|作者|变更说明|
|---|---|---|---|
|V1\.0|2026\-06\-13|个人开发|初版PRD，定义MVP功能、开发范围、迭代规划，适配个人自研学习型项目|

---

## 1\. 项目概述 \& 背景

### 1\.1 项目简介

本项目为**个人自研网络自动化工程项目**，项目名称：**H3C NetCtrl**。

目标：基于 Python FastAPI \+ NETCONF 协议，实现对 H3C 交换机的轻量化 Web 管控，主打**网络自动化开发、NETCONF 协议实操、容器化项目工程落地**。

项目定位：**学习落地项目、工程化练手项目**，非企业生产平台，追求代码规范、流程标准、架构清晰、可稳步迭代。

### 1\.2 前期已完成准备工作

项目启动前本地环境已全部打通，无需重复搭建：

- 使用 **H3C HCL 模拟器** 搭建 Spine\-Leaf 基础网络拓扑，设备互通正常

- 模拟器交换机开启 NETCONF over SSH 服务（830 端口），协议环境就绪

- 搭建专属开发虚拟机，预装 Docker、Docker Compose、SSH 远程开发环境

- 打通虚拟机与 HCL 模拟器网络，确保服务可正常访问设备 NETCONF 接口

### 1\.3 开发模式

- IDE：远程 SSH 开发，服务器容器内调试，全程容器化环境

- 代码管理：纯 Git 命令行工作流（main / dev / feature 分支），轻量敏捷迭代

- 协作人员：个人主导开发，崔协助跟进迭代、联调、测试

### 1\.4 文档说明

本文档为功能需求定义，约束 V1\.0 开发范围、功能、验收标准。开发流程、容器规范、Git 规范、回退机制参考配套《代码开发与敏捷流程规范》。

---

## 2\. V1\.0 MVP 核心目标

**核心一句话目标**：

搭建一套可运行的轻量网控平台，通过 Web 页面完成**单台 H3C 交换机 VLAN 全量增删改查**，具备完整的连接检测、错误捕获、NETCONF 日志调试能力，完成网络自动化基础工程落地。

### 2\.1 P0 必做功能（V1\.0 范围）

|模块|功能点|说明|
|---|---|---|
|设备管理|设备参数配置、连接测试|存储交换机IP、端口、账号密码，支持连通性检测并返回明确错误|
|VLAN 管理|VLAN 查、增、改、删|基于标准 NETCONF get\-config / edit\-config 实现|
|前端页面|极简可视化操作页面|设备信息、VLAN列表、新增编辑表单、错误提示、loading 状态|
|日志调试|完整 NETCONF 报文日志|可开关 DEBUG 日志，打印请求/响应XML，方便排错学习|
|工程部署|容器化一键启动|docker\-compose 统一拉起前后端，环境隔离干净|

### 2\.2 V1\.0 明确不做（避免范围膨胀）

为保证版本快速落地、聚焦核心学习目标，V1\.0 严格不实现以下功能：

- 多设备管理、设备分组、批量操作

- 用户登录、权限、审计日志

- 端口配置、Trunk/Access、二层网络配置

- EVPN、VXLAN、路由、复杂网络业务

- 缓存、消息队列、定时任务、高可用

- 数据导出、图表大屏、报表统计

---

## 3\. 详细功能需求

### 3\.1 设备连接管理

- 支持录入设备名称、IP、NETCONF端口（默认830）、用户名、密码

- 密码做简易加密存储，不明文裸存

- 支持「测试连接」功能，精准返回错误原因：
        

    - 端口不通/拒绝连接

    - 账号密码错误

    - NETCONF服务未开启

    - 会话创建失败

- 数据持久化至 SQLite

### 3\.2 VLAN 核心操作

所有操作兼容 H3C 设备私有 XML 规范，适配模拟器环境。

- **查询VLAN**：拉取设备全部 VLAN ID、名称，前端列表渲染

- **新增VLAN**：限制 1\-4094，名校验，重复拦截

- **修改VLAN**：仅支持修改 VLAN 名称

- **删除VLAN**：指定 VLAN ID 直接删除

所有异常场景（重复创建、不存在、设备报错）统一捕获，前端展示**可读中文错误**，不展示 500 空白报错。

### 3\.3 前端页面设计（极简够用）

- 单页面集成所有功能，轻量化无冗余

- 设备信息展示 \+ 连接测试按钮

- VLAN 数据表格：ID、名称、操作列

- 新增/编辑弹窗表单

- 全局错误提示栏（红色文字）

- 操作 Loading 防重复提交

### 3\.4 后端 API 规范

统一返回格式，结构简洁、方便前端处理。

```json
// 成功
{
  "success": true,
  "data": {}
}

// 失败
{
  "success": false,
  "error": "具体错误描述"
}

```

核心接口清单：

- `GET/POST/PUT /api/device` 设备信息管理

- `POST /api/device/test` 连通性测试

- `GET /api/vlans` 获取VLAN列表

- `POST /api/vlans` 新建VLAN

- `PUT /api/vlans/{vlan_id}` 修改VLAN

- `DELETE /api/vlans/{vlan_id}` 删除VLAN

- `GET /health` 服务健康检查

### 3\.5 日志规范（开发学习核心能力）

本项目重点强化**网络排错、协议调试能力**，日志为核心功能之一。

- 通过环境变量控制 `LOG_LEVEL`（INFO / DEBUG）

- INFO：记录每次操作行为、结果

- DEBUG：打印完整 NETCONF 请求/响应 XML（脱敏）

- 错误日志完整堆栈输出，方便学习排错

---

## 4\. 数据库设计（V1\.0）

采用 SQLite 轻量化存储，无需复杂迁移框架，适配个人小项目。

### 设备表 devices

|字段|类型|说明|
|---|---|---|
|id|INTEGER 主键|自增|
|name|VARCHAR|设备别名|
|host|VARCHAR|设备IP|
|port|INTEGER|默认830|
|username|VARCHAR|登录账号|
|password\_encrypted|VARCHAR|加密密码|
|created\_at / updated\_at|DATETIME|时间记录|

---

## 5\. 部署方式

全程容器化，环境干净、可移植、无本地依赖污染。

- 后端：FastAPI \+ ncclient 容器

- 前端：静态页面 Nginx 容器

- 数据：SQLite 文件挂载宿主机持久化

统一启动命令：

```bash
docker-compose -f docker-compose.dev.yml up -d
```

---

## 6\. 项目迭代规划（个人技术成长路线）

V1\.0 为基础最小可行版本，后续可根据个人学习进度持续迭代，作为**网络自动化 / SDN 开发作品集项目**持续深耕。

### 6\.1 中期迭代（实用功能强化）

- 设备多实例管理、批量操作

- 配置备份、配置对比、版本回滚

- 数据库升级为 MySQL，引入 Alembic 版本管理

- 简单操作日志记录

### 6\.2 进阶网络自动化能力

- Jinja2 配置模板化，实现批量配置下发

- 端口管理、Access/Trunk 配置

- EVPN/VXLAN 基础配置自动化

- 拓扑发现、简易网络可视化

### 6\.3 工程化进阶（简历亮点）

- 引入 Redis、任务队列，处理异步配置任务

- 日志收集、结构化日志输出

- 完善单元测试、接口测试

- CI/CD 自动构建镜像

### 6\.4 远期 AI 辅助方向

- 日志智能排错、错误自动解读

- 自然语言生成网络配置

- 配置异常检测、漂移比对

---

## 7\. V1\.0 验收标准（可落地、可自测）

全部满足即视为 V1\.0 开发完成：

1. 项目容器正常启动，前后端访问正常

2. 成功录入 H3C 模拟器设备，连接测试通过

3. 可正常创建、查询、修改、删除 VLAN，设备配置实时生效

4. 各类错误场景（重复VLAN、连接失败、参数非法）前端均有友好提示

5. DEBUG 模式可完整打印 NETCONF 交互报文，方便学习排错

6. 所有数据持久化，重启容器不丢失设备、VLAN数据

---

## 8\. 补充说明

本项目为**个人学习型工程落地项目**，主打：规范开发、容器化工程、网络自动化协议实战、完整项目生命周期练习。

整体流程轻量化、无企业冗余规范，适合个人迭代、持续打磨、作为技术作品集展示。后续所有升级均基于当前 V1\.0 架构平滑扩展。

> （注：文档部分内容可能由 AI 生成）
