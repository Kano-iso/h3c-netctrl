直接执行：H3C L2VPN/VSI 的 NETCONF XML 实现全景 一、XML 结构体与 H3C CLI 视图的对应关系 h3cdassai\_switch.go（286KB）是自动生成的 XML 结构体文件，每个 Go struct 对应 H3C NETCONF YANG 模型中的一个节点。L2VPN 相关的都在 L2VPN XML 命名空间下： CLI 视图                              XML 结构体（Go struct）           NETCONF 操作 ───────────────────────────────────────────────────────────────────────────────── l2vpn enable                          L2VPNBase / L2VPNBaseInfo         get vsi \<name>                            L2VPNVSIs / L2VPNVSI              get/create/delete vsi \<name> gateway vsi-interface \<id>  L2VPNVSI.VsiInterfaceID           merge interface Vsi-interface\<id>           L2VPNVSIInterfaces / L2VPNVSIInterface  get/create/delete vxlan vxlan-id \<id>                   VXLANs / VXLANsVxlan              get/create vxlan tunnel \<tunnel-id>              VXLANTunnels / VXLANTunnel        get/create evpn encapsulation vxlan              VXLANEvpnVxlanEncaps              get/create arp/nd suppression                    L2VPNVSI.ArpSuppression           merge mac-learning                          L2VPNVSI.MacLearning              merge 二、核心 XML 结构体定义 1. VSI 实例 — L2VPNVSIs（对应 vsi xxx 视图） // h3cdassai\_switch.go:2696 type L2VPNVSIs struct {     XMLName xml.Name   \`xml:"L2VPN"\`         // XML 命名空间     VSIs    \[]L2VPNVSI \`xml:"VSIs>VSI"\`      // 可批量操作 }  type L2VPNVSI struct {     VsiName                 string \`xml:"VsiName,omitempty"\`            // VSI 名称     VsiInterfaceID          string \`xml:"VsiInterfaceID,omitempty"\`     // 绑定的 VSI-Interface     VxlanID                 string \`xml:"VxlanID,omitempty"\`            // VXLAN ID     ArpSuppression          string \`xml:"ArpSuppression,omitempty"\`     // ARP 抑制     NdSuppression           string \`xml:"NdSuppression,omitempty"\`      // ND 抑制     MacLearning             string \`xml:"MacLearning,omitempty"\`        // MAC 学习     MacLimit                string \`xml:"MacLimit,omitempty"\`           // MAC 限制     Flooding                string \`xml:"Flooding,omitempty"\`           // 泛洪     Statistics              string \`xml:"Statistics,omitempty"\`         // 统计     IsL3VNI                 string \`xml:"IsL3VNI,omitempty"\`            // 是否 L3VNI     Dscp                    string \`xml:"Dscp,omitempty"\`               // DSCP     // ... 还有 20+ 个字段 } 2. VSI-Interface — L2VPNVSIInterfaces（对应 interface Vsi-interfacexxx） // h3cdassai\_switch.go:2620 type L2VPNVSIInterfaces struct {     XMLName       xml.Name            \`xml:"L2VPN"\`     VSIInterfaces \[]L2VPNVSIInterface \`xml:"VSIInterfaces>Interface"\` }  type L2VPNVSIInterface struct {     ID                  string \`xml:"ID,omitempty"\`                  // VSI-Interface ID     IfIndex             string \`xml:"IfIndex,omitempty"\`             // 接口索引     Name                string \`xml:"Name,omitempty"\`                // 接口名     L3VNI               string \`xml:"L3VNI,omitempty"\`               // 关联的 L3VNI     PrefixRouteGenerate string \`xml:"PrefixRouteGenerate,omitempty"\` // 前缀路由生成     // ... } 3. VXLAN 绑定 — VXLANs（对应 vxlan vxlan-id xxx） // h3cdassai\_switch.go:6596 type VXLANs struct {     XMLName xml.Name      \`xml:"VXLAN"\`     VXLANs  \[]VXLANsVxlan \`xml:"VXLANs>Vxlan"\` }  type VXLANsVxlan struct {     VxlanID   string \`xml:"VxlanID,omitempty"\`   // VXLAN ID     VsiName   string \`xml:"VsiName,omitempty"\`   // 关联的 VSI     TunnelAll string \`xml:"TunnelAll,omitempty"\`  // 全量隧道 } 4. VXLAN Tunnel — VXLANTunnels（对应 vxlan tunnel xxx） // h3cdassai\_switch.go:6541 type VXLANTunnels struct {     XMLName xml.Name      \`xml:"VXLAN"\`     Tunnels \[]VXLANTunnel \`xml:"Tunnels>Tunnel"\` }  type VXLANTunnel struct {     VxlanID       string \`xml:"VxlanID,omitempty"\`       // VXLAN ID     TunnelID      string \`xml:"TunnelID,omitempty"\`      // Tunnel 接口 ID     FloodingProxy string \`xml:"FloodingProxy,omitempty"\`  // 泛洪代理     // 还有统计字段 InPkts, OutPkts, InOctets, OutOctets 等 } 5. EVPN VXLAN — VXLANEvpnVxlanEncaps // h3cdassai\_switch.go:6494 type VXLANEvpnVxlanEncaps struct {     XMLName         xml.Name              \`xml:"VXLAN"\`     EvpnVxlanEncaps \[]VXLANEvpnVxlanEncap \`xml:"EvpnVxlanEncaps>VxlanEncap"\` }  type VXLANEvpnVxlanEncap struct {     VsiName       string \`xml:"VsiName,omitempty"\`       // VSI 名称     RD            string \`xml:"RD,omitempty"\`             // Route Distinguisher     BRDAuto       string \`xml:"bRDAuto,omitempty"\`        // 自动 RD     BIrtAuto      string \`xml:"bIrtAuto,omitempty"\`       // 自动 IRT     BErtAuto      string \`xml:"bErtAuto,omitempty"\`       // 自动 ERT     BMacAdvertise string \`xml:"bMacAdvertise,omitempty"\`  // MAC 通告     MappingVni    string \`xml:"MappingVni,omitempty"\`     // 映射 VNI } 三、实际 NETCONF 操作流程 以 L2VsiInterfaceCreate 为例，每步操作都是 先 GET 查询，再决定 CREATE/跳过 的幂等模式： // h3cdassai\_l2vni.go  // 步骤1: 创建 Vsi-interface（如果不存在） l2VPNVSIInterfaces := L2VPNVSIInterfaces{...} resp, \_ := r.H3cGet(ctx, l2VPNVSIInterfaces)    // NETCONF \<get> — 查询是否存在 if resp.Size() == 0 {     r.H3cCreate(ctx, l2VPNVSIInterfaces)          // NETCONF \<edit-config> create — 创建 }  // 步骤2: 绑定 VRF（如果不存在） l3vpnIf := L3vpnIf{...} resp, \_ = r.H3cGet(ctx, l3vpnIf) if resp.Size() == 0 {     r.H3cCreate(ctx, l3vpnIf)                     // 对应 CLI: ip binding vpn-instance xxx }  // 步骤3: 设置 MAC（内部固定 MAC ee-ff-ff-ff-ff-ff） ifmgrInterfaces := IfmgrInterfaces{...} resp, \_ = r.H3cGet(ctx, ifmgrInterfaces) if resp.Size() == 0 {     r.H3cCreate(ctx, ifmgrInterfaces)             // 对应 CLI: mac-address ee-ff-ff-ff-ff-ff }  // 步骤4: 配置 IPv4 地址 // 先查，如果已存在但地址不一致 → 报错；如果不存在 → 创建  // 步骤5: 配置 IPv6 地址 + Link Local 地址 // 同上，先查后建  // 步骤6: 创建 VSI 实例 l2vnpVsi := L2VPNVSIs{VSIs: \[]L2VPNVSI{{VsiName: l2Vni, VsiInterfaceID: vsiIdx}}} resp, \_ = r.H3cGet(ctx, l2vnpVsi) if resp.Size() == 0 {     r.H3cCreate(ctx, l2vnpVsi)                    // 对应 CLI: vsi xxx }  // 步骤7: 绑定 VXLAN vsiVxlan := VXLANs{VXLANs: \[]VXLANsVxlan{{VsiName: l2Vni, VxlanID: l2Vni}}} resp, \_ = r.H3cGet(ctx, vsiVxlan) if resp.Size() == 0 {     r.H3cCreate(ctx, vsiVxlan)                    // 对应 CLI: vxlan vxlan-id xxx } 四、实际 NETCONF XML 报文长什么样 以创建 VSI 为例，H3cCreate 内部会生成类似这样的 NETCONF RPC： \<rpc message-id="1" xmlns="urn:ietf:params:xml:ns:netconf:base:1.0">   \<edit-config>     \<target>\<running/>\</target>     \<error-option>rollback-on-error\</error-option>     \<config>       \<top xmlns="http\://www\.h3c.com/netconf/config:1.0"            nc:operation="create"            xmlns:nc="urn:ietf:params:xml:ns:netconf:base:1.0">         \<L2VPN>           \<VSIs>             \<VSI>               \<VsiName>vsi\_10001\</VsiName>               \<VsiInterfaceID>9102\</VsiInterfaceID>               \<Statistics>true\</Statistics>             \</VSI>           \</VSIs>         \</L2VPN>       \</top>     \</config>   \</edit-config> \</rpc> 五、完整 XML 结构体清单（L2VPN 命名空间） Go Struct	CLI 对应	支持操作 L2VPNBase	display l2vpn	get L2VPNVSIs	vsi xxx	get/create/delete/merge L2VPNVSIInterfaces	interface Vsi-interfacexxx	get/create/delete L2VPNACs	AC 附着电路	get L2VPNPWClasses	PW 模板	get L2VPNLocalMACs	本地 MAC 表	get L2VPNFloodingMACs	泛洪 MAC 表	get L2VPNVSIStatistics	VSI 统计	get L2VPNVSIIpv4Subnets	VSI IPv4 子网	get L2VPNVSIIpv6Subnets	VSI IPv6 子网	get L2VPNL3Interfaces	L3 接口绑定	get L2VPNInterfaceConfigs	接口配置	get L2VPNClearVSIMAC	清除 VSI MAC	action L2VPNClearVSIStatistics	清除 VSI 统计	action VXLANs	vxlan vxlan-id	get/create VXLANTunnels	vxlan tunnel	get/create VXLANEvpnVxlanEncaps	evpn encapsulation	get/create VXLANEvpnVxlanRTs	EVPN RT	get/create VXLANRmtMACs	远端 MAC	get  深度交互 这套 YANG 模型驱动的 NETCONF 实现有以下特点： 幂等性是应用层保障的，而非依靠 NETCONF。每步都是 GET → 判断 → CREATE/跳过，这是为了避免交换机因重复配置而报错。  h3cdassai\_switch.go 是自动生成的（开头注释 auto generated by code from doc），直接从 H3C 的 YANG 模型文件生成 Go struct，这意味着如果你需要新的 L2VPN 字段（比如 gateway、arp-suppression 等），只需要更新 YANG 源文件然后重新生成即可。  L3VNI 和 L2VNI 是两套路径：h3cdassai\_l2vni.go 是后来新增的二层 VNI 支持，而 h3cdassai.go 中的 l3VxlanRifCreateInternal 是三层 VSI 接口的创建（包含 VRF 绑定、IP 地址等）。两者在底层都操作 VSI 和 Vsi-Interface，但上层调用的封装不同。
