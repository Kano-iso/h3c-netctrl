# H3C NetCtrl V3.4 PRD：前端大屏 + UX

| 版本 | 日期 | 作者 | 说明 |
|---|---|---|---|
| V3.4 Draft | 2026-07-18 | 用户拍板 + Codex 共创 | V3.4 = 前端大屏 + UX，sdn-visual-overview + sdn-ops-toolkit-probes 集中实现 |

---

## 1. 背景与目标

### 1.1 背景

v3.0 / v3.2 / v3.3 后端能力已落地，但**前端展示分散**：
- Devices.vue：基础设备列表
- CMDB.vue：资产列表
- 终端 / 接口 / VPN：各自独立页面
- **没有统一的"网络拓扑 + 状态全景"视图**

### 1.2 目标

**前端大屏 + UX 集中实现**：
1. VPC 详情页（VPC / 端口 / 网关 / 路由 / 状态全景）
2. 端口矩阵（所有设备 × 所有端口 × VPC 归属 / UP/DOWN）
3. 网络拓扑（underlay + overlay 拓扑图）
4. UX 打磨（参考用户原话："前端逻辑很重要... 考虑到前端到时候怎么逻辑，考虑到用户体验"）

### 1.3 用户原话

> "3.4 做前端其实我觉得这个前端的逻辑也很重要啊，这也是我们之前提到的，要在 3.0 版本和 3.3 版本的时候，做后端功能的时候，考虑到前端到时候怎么逻辑，考虑到用户体验，考虑到各种的不一样的情况。当然你也不可能往十全十美，这个在后面再考虑吧"

## 2. V3.4 范围

### 2.1 VPC 详情页（sdn-visual-overview 主体）

#### 2.1.1 目标

**单一 VPC 全景视图**：展示该 VPC 涉及的所有资源 + 状态 + 拓扑。

#### 2.1.2 范围

- **VPC 基本信息**：name / rd / rt / vni / 描述
- **关联设备**：所有 leaf / spine 设备 + 角色（active 网关 / transit / spine）
- **端口列表**：该 VPC 绑定的所有端口 + 状态（UP/DOWN/被占用）
- **VSI / Vsi-interface**：所有 VSI 名称 + 接口 + IP
- **EVPN 路由**：L2VNI / L3VNI 路由数量 + 健康度
- **状态图**：环形图（UP / DOWN / 异常 比例）
- **时间线**：VPC 创建 / 端口绑定 / 状态变更 历史

#### 2.1.3 验收标准

- [ ] VPC 详情页能展示完整资源 + 状态
- [ ] 数据实时刷新（5s interval）
- [ ] 支持深链（VPC ID 直接定位）

### 2.2 端口矩阵

#### 2.2.1 目标

**所有设备 × 所有端口 × VPC 归属** 一目了然。

#### 2.2.2 范围

- **设备列表**：所有受管设备
- **端口列表**：每个设备的端口（按设备 vendor / model 适配）
- **VPC 归属色**：每个端口按 VPC 着色（无 VPC = 灰色）
- **状态指示**：UP（绿）/ DOWN（红）/ 异常（黄）
- **交互**：点击端口 → 弹窗显示 VPC 详情 / 端口历史
- **过滤**：按设备 / 按 VPC / 按状态

#### 2.2.3 验收标准

- [ ] 端口矩阵页面能展示所有设备 + 所有端口
- [ ] VPC 归属色正确（按 service-instance 配置）
- [ ] 状态实时刷新
- [ ] 性能：100 设备 × 48 端口 = 4800 单元格，渲染 < 3s

### 2.3 网络拓扑

#### 2.3.1 目标

**underlay + overlay 拓扑图**，可视化网络结构。

#### 2.3.2 范围

- **underlay 拓扑**：
  - spine / leaf 设备
  - 物理连接（LLDP / CDP 发现）
  - OSPF / BGP 邻居关系
- **overlay 拓扑**：
  - VPC 边界
  - EVPN type-2/3/5 路由
  - VXLAN tunnel
- **交互**：
  - 拖拽 / 缩放
  - 点击节点 → 设备详情
  - 点击链路 → 端口/VPC 详情
- **可视化库**：d3.js / vis.js / echarts（v3.4 评估选型）

#### 2.3.4 验收标准

- [ ] underlay 拓扑正确显示
- [ ] overlay 拓扑正确显示
- [ ] 交互流畅（拖拽 / 缩放 / 点击）

### 2.4 UX 打磨

#### 2.4.1 目标

按用户原话"考虑到用户体验"，集中打磨 5 个核心场景的 UX。

#### 2.4.2 范围

- **设备纳管流程**：
  - `POST /api/devices` 简化（v3.1.3 自动纳管后）
  - 错误提示可读化（带 error_key + 修复建议）
- **VPC 创建流程**：
  - 5 步向导（基本信息 / 设备选择 / 端口绑定 / 配置计划 / 确认）
  - dry-run 预览（planned_config）
  - 错误回滚（任意步骤失败可回退）
- **状态展示**：
  - 统一状态色（UP 绿 / DOWN 红 / 异常 黄 / UNKNOWN 灰）
  - 状态变更动画（避免突兀跳转）
- **批量操作**：
  - 多选设备 → 批量备份 / 批量采集
  - 多选 VPC → 批量删除
- **错误处理**：
  - 网络错误友好提示（带重试按钮）
  - 设备不可达 → "该设备当前不可达，是否重试？"

#### 2.4.3 验收标准

- [ ] 5 步 VPC 创建向导完成
- [ ] dry-run 预览 100% 准确
- [ ] 批量操作可用
- [ ] 错误提示带修复建议

### 2.5 ops-toolkit-probes（VPC/EVPN 专用探测）

#### 2.5.1 目标

按真实调试痛点补工具，**不发散**（用户原话："按真实调试痛点补工具，不提前发散"）。

#### 2.5.2 范围

ops-toolkit 加 3 个 VPC/EVPN 专用脚本：
- `sdn-vpc-status.sh`：单设备 VPC 状态（VSI / EVPN / L3VPN / Vsi-interface）
- `sdn-evpn-routes.sh`：单设备 EVPN 路由表（L2VNI / L3VNI / type-2/3/5）
- `sdn-vxlan-tunnels.sh`：单设备 VXLAN tunnel 状态

复用现有 paramiko-batch-exec.sh 的 4 级凭据 + Fernet 密文 + JSON 输出规范。

#### 2.5.3 验收标准

- [ ] 3 个脚本能跑通
- [ ] 在 .5 / .26 真机验证
- [ ] 输出 JSON 结构稳定

## 3. 走法（4 阶段）

| 阶段 | 主题 | 输出 | 依赖 |
|---|---|---|---|
| **阶段 1** | VPC 详情页 | 单一 VPC 全景视图 | v3.2 / v3.3 后端 |
| **阶段 2** | 端口矩阵 + 网络拓扑 | 大屏视图 | 阶段 1 |
| **阶段 3** | UX 打磨 | 5 步向导 + 错误处理 | 阶段 1+2 |
| **阶段 4** | ops-toolkit-probes | 3 个脚本 | 阶段 1（部分） |

## 4. 不做（明确边界）

- ❌ **不做 etcd 协调**（v3.5 远期）
- ❌ **不做移动端**（v3.4 仅桌面端）
- ❌ **不做实时监控 / 告警**（monitor 远期独立 PRD）
- ❌ **不做 AI 辅助**（远期）

## 5. 依赖关系

- 依赖 v3.2 / v3.3 后端
- 依赖 v2.6.0 i18n（中英文）

## 6. 风险

- **风险 1：可视化库选型不成熟**：d3.js / vis.js / echarts 都有学习成本
  - 缓解：先 PoC 3 个库，选最合适的
- **风险 2：端口矩阵性能**：100 设备 × 48 端口 = 4800 单元格
  - 缓解：虚拟滚动 + 按需渲染
- **风险 3：UX 打磨需求蔓延**：用户可能不断提新需求
  - 缓解：v3.4 定稿后冻结 UX 范围，新需求推到 v3.5+
